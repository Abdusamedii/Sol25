import { signinSchema, signupSchema, userSchema } from '@sol25/shared';

export const signupBodySchema = signupSchema;
export const signinBodySchema = signinSchema;
export const userResponseSchema = userSchema;
